specVersion: "v1beta"
serviceId: "webfolder"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webfolder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
